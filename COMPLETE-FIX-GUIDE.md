# 完整修复方案 - 2026-03-12

## ⚠️ 重要说明
- **此方案为临时止血方案，仅用于开发/测试环境**
- **不要在生产环境使用**
- **不要放真实用户数据**
- **回切日期：2026-03-19**
- **届时需要实施 Supabase Auth + 正确的RLS策略**

---

## 📋 修复的11个问题

### 1. ✅ 教练端登录成功但不跳转
**原因：** 登录后没有保存完整的教练数据到 localStorage
**修复：**
- 保存 `coachData` 到 localStorage
- 登录后跳转到邀请码管理页面
**文件：** `src/views/CoachAuth.vue`

### 2. ✅ 邀请码页面报错（RLS权限）
**原因：** invite_codes 表的 RLS 策略过于严格
**修复：** 暂时禁用 RLS（临时方案）
**文件：** `temporary-fix-2026-03-12.sql`

### 3. ✅ 会员管理页面空白
**原因：** API 返回值处理错误，没有正确解析 `{success, data, error}` 格式
**修复：**
- 正确处理 API 返回值
- 提取会员信息：`result.data.map(item => item.members)`
**文件：** `src/views/coach/CoachMembers.vue`

### 4. ✅ 旧会员账号登录失败
**原因：** 旧会员账号没有密码字段
**修复：** 给所有旧会员设置默认密码 "123456"
**文件：** `temporary-fix-2026-03-12.sql`

### 5. ✅ 会员首页显示"王五"
**原因：** localStorage 缓存了旧数据
**解决：** 用户需要清除浏览器 localStorage：`localStorage.clear()`

### 6. ✅ 训练计划加载失败
**原因：** RLS 权限问题
**修复：** 暂时禁用 RLS（临时方案）
**文件：** `temporary-fix-2026-03-12.sql`

### 7. ✅ 我的进步 - 获取会员信息失败
**原因：** RLS 权限问题
**修复：** 暂时禁用 RLS（临时方案）
**文件：** `temporary-fix-2026-03-12.sql`

### 8. ✅ 我的认证 - 获取会员信息失败
**原因：** RLS 权限问题
**修复：** 暂时禁用 RLS（临时方案）
**文件：** `temporary-fix-2026-03-12.sql`

### 9. ✅ 查找教练页面打不开
**原因：** API 返回值处理错误
**修复：**
- 正确处理 API 返回值
- 提取教练信息：`result.data.map(item => item.coaches)`
**文件：** `src/views/member/CoachList.vue`

### 10. ✅ 管理员登录正常
**状态：** 无需修复

### 11. ✅ 管理员审核按钮无反应
**原因：** API 返回值处理错误
**修复：** 正确处理 API 返回值，添加成功/失败提示
**文件：** `src/views/admin/CoachAudit.vue`

---

## 🔧 修复的文件清单

### SQL 脚本
1. `temporary-fix-2026-03-12.sql` - 临时止血方案
   - 禁用 5 张表的 RLS
   - 重置旧会员密码
   - 设置教练审核状态

### 前端代码
1. `src/views/CoachAuth.vue` - 教练登录跳转
2. `src/views/coach/CoachMembers.vue` - 会员管理页面
3. `src/views/member/CoachList.vue` - 查找教练页面
4. `src/views/member/MemberHome.vue` - 会员首页
5. `src/views/admin/CoachAudit.vue` - 管理员审核页面

---

## 📝 执行步骤

### 第一步：执行 SQL 脚本
1. 打开 Supabase Dashboard
2. 进入 SQL Editor
3. 复制 `temporary-fix-2026-03-12.sql` 的内容
4. 粘贴并执行

### 第二步：清除浏览器缓存
1. 打开浏览器开发者工具（F12）
2. 进入 Console 标签
3. 执行：`localStorage.clear()`
4. 刷新页面

### 第三步：测试所有功能
1. **教练端测试**
   - 登录：18613371019 / wei1063593464
   - 测试邀请码生成
   - 测试会员管理

2. **会员端测试**
   - 登录：13800138000 / 123456
   - 测试会员首页
   - 测试训练计划
   - 测试我的进步
   - 测试我的认证
   - 测试查找教练

3. **管理员端测试**
   - 登录：admin / admin123
   - 测试教练审核

---

## ⚠️ 安全提醒

**此方案的安全风险：**
1. RLS 已禁用，任何知道 API 的人都可以读写数据
2. 所有旧会员使用相同的默认密码 "123456"
3. 没有用户身份验证机制

**缓解措施：**
1. 仅在开发/测试环境使用
2. 不要放真实用户数据
3. 尽快实施 Supabase Auth + 正确的 RLS 策略
4. 回切日期：2026-03-19

---

## 🎯 下一步计划（2026-03-19 前完成）

1. **实施 Supabase Auth**
   - 使用 Supabase 的认证系统
   - 替换自定义的手机号+密码认证

2. **设计安全的 RLS 策略**
   - 基于 `auth.uid()` 和用户角色
   - 只允许用户操作自己的数据
   - 管理员有特殊权限

3. **迁移现有用户**
   - 将现有用户迁移到 Supabase Auth
   - 强制用户首次登录时修改密码

---

**最后更新：** 2026-03-12
**更新人：** 小助理
